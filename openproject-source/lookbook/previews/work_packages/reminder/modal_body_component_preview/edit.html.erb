<%=
  render(Primer::Alpha::Dialog.new(title: "Edit reminder")) do |d|
    d.with_show_button(icon: :clock, "aria-label": "Edit reminder")
    d.with_body do
      render(WorkPackages::Reminder::ModalBodyComponent.new(remindable:, reminder:, errors:))
    end
  end
%>

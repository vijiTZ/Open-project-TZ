<%=
  render(Primer::Alpha::Dialog.new(title: "Create reminder")) do |d|
    d.with_show_button(icon: :clock, "aria-label": "Create reminder")
    d.with_body do
      render(WorkPackages::Reminder::ModalBodyComponent.new(remindable:, reminder:, errors:))
    end
  end
%>

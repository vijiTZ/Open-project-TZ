<%= turbo_frame_tag "portfolios_sidemenu",
                    src: portfolios_menu_path(
                      controller_path:,
                      **params.permit(:filters, :sortBy, :id, :query_id)
                    ),
                    target: "_top",
                    data: { turbo: false },
                    loading: :lazy %>

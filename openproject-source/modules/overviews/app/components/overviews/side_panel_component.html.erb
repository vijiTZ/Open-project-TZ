<%=
  component_wrapper do
    render(Primer::OpenProject::SidePanel.new(spacious: true)) do |panel|
      [
        Overviews::ProjectCustomFields::SidePanelComponent.new(project: @project),
        Overviews::ProjectPhases::SidePanelComponent.new(project: @project)
      ].each do |component|
        panel.with_section(component)
      end
    end
  end
%>

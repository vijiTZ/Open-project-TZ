<%=
  component_wrapper do
    render(
      Primer::OpenProject::SidePanel.new(
        spacious: true,
        test_selector: "project-life-cycle-sidebar-async-content"
      )
    ) do |panel|
      panel.with_section(Overviews::ProjectPhases::ShowComponent.new(project: @project))
    end
  end
%>

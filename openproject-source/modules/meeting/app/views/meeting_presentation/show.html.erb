<% html_title "#{t(:label_meeting)}: #{@meeting.title} - #{t('meeting.presentation_mode.title')}" %>

<%=
  render Meetings::PresentationMode::ShowComponent.new(
    meeting: @meeting,
    sorted_agenda_item_ids:,
    current_id: @current_id,
    started_at: @started_at
  )
%>
